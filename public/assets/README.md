# Site images

Place new marketing and quarry photos here using **lowercase, hyphenated filenames** (e.g. `hero-quarry.jpg`).

Update `src/app/data/site-assets.ts` when adding files.

Do **not** put large images in `src/assets/` — that folder is watched by the dev server and can break `ng serve`.
